; Dos Navigator Editor configuration file

exe: hiew !.!
com: hiew !.!
*: hiew !.!

;???: [Path]Name !.!
;                 запускает редактор с параметром. В качестве параметра
; │       │       └─ могут выступать макросы типа !.!, которые описаны в
; │       │          разделе МЕНЮ|Формат пользовательское меню.
; │       └──────── [полное]имя внешнего редактора
; └───────────── расширения для этого редактора (pазделяются запятыми)

;    Пример: PAS:  D:\BP\BP !
;            CPP:  D:\BC\BC !